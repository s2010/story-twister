# Story-Twister Production Environment Template
# Copy this to .env and fill in your production values
# NEVER commit the actual .env file to version control

# =============================================================================
# 🔐 CRITICAL SECRETS - REPLACE ALL VALUES BELOW
# =============================================================================

# Database Configuration (REQUIRED)
DATABASE_URL=postgresql+asyncpg://prod_user:REPLACE_WITH_STRONG_PASSWORD@your-db-host:5432/story_twister_prod

# Admin Credentials (REQUIRED - Use strong values)
ADMIN_SEED_USERNAME=admin
ADMIN_SEED_EMAIL=admin@yourdomain.com
ADMIN_SEED_PASSWORD=REPLACE_WITH_STRONG_PASSWORD_24_CHARS_MIN
ADMIN_SEED_API_TOKEN=REPLACE_WITH_SECURE_TOKEN_48_CHARS_MIN

# AI Service API Key (REQUIRED for story generation)
GROQ_API_KEY=REPLACE_WITH_YOUR_GROQ_API_KEY

# Security Configuration
JWT_SECRET=REPLACE_WITH_JWT_SECRET_32_CHARS_MIN
ADMIN_JWT_SECRET=REPLACE_WITH_ADMIN_JWT_SECRET_32_CHARS_MIN

# =============================================================================
# 🌐 PRODUCTION CONFIGURATION
# =============================================================================

# Environment
NODE_ENV=production
VITE_NODE_ENV=production

# CORS Configuration (CRITICAL - Only allow your production domains)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://api.yourdomain.com

# API Configuration
VITE_API_BASE_URL=https://api.yourdomain.com

# =============================================================================
# 📊 OPTIONAL CONFIGURATION
# =============================================================================

# Redis (if using for caching/sessions)
# REDIS_URL=redis://localhost:6379

# Email Service (if implementing notifications)
# EMAIL_SERVICE_API_KEY=your_email_service_key
# FROM_EMAIL=noreply@yourdomain.com

# Monitoring & Analytics
# SENTRY_DSN=your_sentry_dsn
# GOOGLE_ANALYTICS_ID=your_ga_id

# Rate Limiting
# RATE_LIMIT_REQUESTS_PER_MINUTE=60

# File Upload (if implemented)
# MAX_FILE_SIZE_MB=10
# UPLOAD_STORAGE_PATH=/var/uploads

# =============================================================================
# 🚨 SECURITY NOTES
# =============================================================================
# 
# 1. Generate strong passwords using: python -c "import secrets, string; print(''.join(secrets.choice(string.ascii_letters + string.digits + '!@#$%^&*') for _ in range(24)))"
# 2. Generate secure tokens using: python -c "import secrets; print(secrets.token_urlsafe(48))"
# 3. Never use default/example values in production
# 4. Rotate credentials regularly
# 5. Use environment-specific database URLs
# 6. Restrict CORS to your actual domains only
# 7. Enable HTTPS in production
# 8. Monitor logs for security issues
#
# =============================================================================
